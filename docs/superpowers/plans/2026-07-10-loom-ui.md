# Loom UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Craft tasks — verify by running the app (`npm run dev` in frontend/ + server), not by unit tests.

**Goal:** Make DreamWeave's frontend the most memorable demo in the field: an animated thread-weave landing page and a live "Loom" run view where real CROO hires, agent births, and the proof tree play out visually.

**Architecture:** Keep the existing design system (global.css tokens: amber `#ffb020` on near-black, Bricolage Grotesque/Inter/JetBrains Mono, dual theme, grain). Add motion centerpieces with framer-motion (already installed) + hand-rolled SVG. No new dependencies.

**Inspiration fusion:** ZKredit (glow-on-dark, display/mono type contrast, gradient washes) + CROO's own sites (numbered editorial chapters `01 /`, smallcaps mono labels, slash separators) + our thread/loom visual system.

## Global Constraints

- Dashboard nav/labels stay plain language (user preference): "Projects", "Agents", "Payments", "Live run" — themed words (Loom, dreams, weave) belong to the landing page and decorative copy only.
- `prefers-reduced-motion` respected everywhere (global.css already guards CSS; gate framer-motion loops with `useReducedMotion`).
- Dual theme must keep working — use existing CSS vars, never hardcoded colors.
- 60fps: animate only transform/opacity/stroke-dash properties; no layout animation in loops.
- No new deps beyond what's installed (framer-motion, react-router).

---

### Task 1: SSE + API plumbing for new events

**Files:** `frontend/src/lib/useRun.ts`, `frontend/src/lib/api.ts`, `server/index.ts`

- `useRun`: capture `birth` events into `births: { name, capabilityId, storeUrl }[]` and `prooftree` event into `prooftree: { root, leaves }`; expose both.
- `api.ts`: add `prooftree(dreamId)` → `GET /api/dreams/:id/prooftree`; add `royalties()` → `GET /api/royalties`.
- `server/index.ts`: add public `GET /api/royalties` returning `{ royalties: [{ childAgentId, childName, orderRef, amountUsdc, createdAt }] }` (join names via `getAgent`).
- Verify: `npm run typecheck`, boot server, `curl /api/royalties`.

### Task 2: Landing page — the woven hero

**Files:** `frontend/src/pages/Landing.tsx`, new `frontend/src/components/ThreadField.tsx`, `frontend/src/styles/app.css`

- `ThreadField`: full-bleed SVG behind the hero — 5–7 amber bezier threads that draw themselves in (stroke-dashoffset animation, staggered), converge toward a hub, and gently drift (subtle path morph via animated control points). Reduced-motion: static drawn state.
- Hero copy (editorial, Clash-style with our Bricolage): headline "Hire a team, not an agent." / subline "DreamWeave hires real agents from the CROO store — and when the right one doesn't exist, it gives birth to it. Every delivery arrives with proof."
- CTA row: primary "Cast your first project" → app; ghost "View on CROO Store" → our real listing.
- Chapters `01 / Plan  02 / Hire  03 / Birth  04 / Prove` — scroll-reveal (whileInView), each with a small looping vignette (mono captions, slash separators like cap.croo.network).
- Live proof strip: real numbers — store orders settled, agents born, proof roots minted (from `/api/stats` + extend stats with births/royalties counts if trivial).
- Verify visually in browser, both themes.

### Task 3: The Loom — run view upgrade

**Files:** `frontend/src/components/RunView.tsx` (major), `frontend/src/styles/app.css`

- Thread lanes: each hire is a horizontal lane; an amber thread spools left→right as phases advance (match→negotiate→lock→deliver→clear), with the phase stations as nodes that light up. Store hires get a `CROO` pill; sim/local get none.
- Settle pulse: on `clear`, a USDC glyph pulses along the thread; on `void`, thread snaps coral.
- TEE seal: deliveries with attestation stamp a small hexagonal seal on the lane (scale+glow spring).
- **Birth moment** (the money shot): when a `birth` event arrives, insert a full-width card between lanes — forge-burst animation (radial glow + particles via framer-motion), newborn's name in display type, capability, and a live link "→ see it on the CROO store". Keep it big enough to carry the demo video.
- Prooftree panel: when the `prooftree` event lands, show root hash (mono, copy button) and an expandable leaf list — each leaf: role badge (hired/born/local), agent, order id, links: Base tx (basescan.org/tx/…) when payTxHash, store order when serviceId real, TEE badge when attestation.
- Verify: run a local sim dream end-to-end in the browser; confirm lanes animate and prooftree renders (sim leaves have no tx links — fine).

### Task 4: Payments page — royalty streams

**Files:** `frontend/src/pages/Payments.tsx`

- Section "Royalties" under existing content: table of royalty rows (child, order, amount, when) + a per-child animated flow strip (small amber dots drifting child→Foundry). Plain-language heading: "Royalties — 10% of every born agent's earnings".
- Verify with seeded/local data (record one royalty via a dev call if needed).

### Task 5: Polish + meta pass

**Files:** `frontend/index.html`, `frontend/src/components/AppShell.tsx`, various

- index.html: title "DreamWeave — hire a team, not an agent", meta description, favicon from the logo mark (SVG data URI), og tags.
- AppShell: active-nav thread underline (layoutId motion), page-mount fade/rise transitions.
- Sweep: consistent spacing, empty states, loading skeletons on new panels.
- Final check: typecheck, `vite build` clean, both themes, mobile width sanity (390px), reduced-motion pass.
